# Build artifacts (only in build directory)
/build/*
!/build/rebuild.sh

# Build outputs in subdirectories
build/*.o
build/*.a
build/*.so
build/*.dylib
build/main

# Hash cache
.hash/

# IDE
.vscode/
.idea/

# OS
.DS_Store
